###
### PostgreSQL
###

sudo apt-get -y install postgresql libpq-dev

su -l -c "createuser -P -s vagrant" postgres

echo "Server: sudo service postgresql start - (it's already up though)"
echo "Create db: createdb <dbname>"
echo "Client: psql <dbname>"
