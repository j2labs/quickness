###
### Django
###
### To create an actual django application you should create
### a <project_name>.private file in the formula directory
### and source '. django <project_name>' this will create
### a virtualenv with the name of the project.  Also
### remember to create a etc/private/django.requirements.
###

###
### Initialize
###

if [ "$(is_formula_installed)" = "yes" ]; then
    quickness_teardown
    return
fi

sudo cp $QUICKNESS_ETC/django_bash_completion /etc/bash_completion.d

APP_NAME="django"
if [ ! -z "$1" ]; then
    APP_NAME="$1"
fi


###
### Dependencies
###

. $Q/python_dev $APP_NAME
. $Q/python_imaging


###
### Installation
###

if [ -f $QUICKNESS_PRIVATE/django.requirements ]; then
    pip install -r $QUICKNESS_PRIVATE/django.requirements
else
    quickness_msg "using default requirements, create etc/private/django.requirements to load in your own requirements"
    pip install -r $QUICKNESS_ETC/django.requirements
fi


###
### Teardown
###

quickness_teardown
