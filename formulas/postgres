###
### PostgreSQL
###

###
### Initialize
###

if [ "$(is_formula_installed)" = "yes" ]; then
    quickness_teardown
    return
fi


###
### Installation
###

sudo apt-get -y install \
    postgresql \
    libpq-dev

sudo su -l -c 'createuser -s $USER' postgres


###
### Teardown
###

quickness_msg "
  Server: sudo service postgresql start - (it's already up though)
  Create db: createdb <dbname>
  Client: psql <dbname>

  This formula creates a superuser named $USER to make development easier"

quickness_teardown
